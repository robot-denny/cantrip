diff --git a/importer/events.py b/importer/events.py
index 5f1b0a3..c7e2d94 100644
--- a/importer/events.py
+++ b/importer/events.py
@@ -18,6 +18,15 @@ class EventImporter:
         self.publisher = publisher
         self.log = log

+    def publish_all(self, events):
+        for event in events:
+            try:
+                self.publisher.publish(event)
+            except PublishFailure:
+                self.log.warning("Could not publish event")
+                continue
+        return len(events)
+
     def load(self, path):
         with open(path) as handle:
             return json.load(handle)
